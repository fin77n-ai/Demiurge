# BRIEF: 架构与业务数据流向图生成器

> **目标**：重构 `SourceAnalyzerModal` 的后端解析器 `ai/wireframe_generator.py`。不再生成抽象且不贴合的 UI 页面线框图与简易流程图，而是生成高度定制化的“模块结构树”与“业务数据流管道”一页式架构双栏大图，直观展现物理代码到逻辑数据流的映射。
>
> 核心哲学：在写代码之前或重构代码时，协助用户理清“物理目录结构”与“逻辑数据管道”之间的绑定关系。

---

## 1. 本次目标

让“从代码生成线框图”工具彻底蜕变。当用户拖入文件夹或输入 GitHub 地址时，Demiurge 能够输出如下精美的 Excalidraw 画布图：
- **左栏**：📁 核心项目结构树。带缩进、树状连接线（`├──`、`└──`），且每个关键文件右侧都有圆括号标注的中文职责描述。
- **右栏**：⚙️ 业务数据流与架构管道。以带圆角、带主题颜色背景的泳道卡片垂直排列，通过 Excalidraw 连线（带双向绑定）串联。卡片内写明该阶段的数据流向以及调用的具体物理代码文件。

**回答这个：这是在帮用户更好地"想清楚"吗？还是在自动化掉用户的思考？**
这是在极大地帮助用户在宏观层面上“想清楚”物理代码如何承载逻辑数据流，把松散的文件和抽象的过程紧密结合，从而做对 Stage 1 架构规划，而不是去生成毫无意义的空洞 UI 组件。

---

## 2. 改动范围（精确到文件 + 函数）

**唯一允许修改的文件**：
- `ai/wireframe_generator.py`

**允许修改的函数**：
- `generate_wireframe` — 彻底重构：
  1. 保留 `doctor_analysis` 以便做基础体检（项目类型和 notes）。
  2. 新增或合并 `analyze_architecture_and_pipeline` 做统一深度 AI 解析。
  3. 重建 Excalidraw element 生成算法，按双栏（树状结构 text + 数据流 pipeline card）精确定位绘制。
  4. 完美兼容前端 `pages` 元数据返回契约，将 pipeline steps 映射为 `pages`，保证前端 React（如 `SourceAnalyzerModal.jsx`）无需修改任何代码。

**允许新增的函数**：
- `analyze_architecture_and_pipeline` — 呼叫大模型，以 JSON 形式一次性输出格式化项目树、数据管道步骤、卡片配置。

---

## 3. 不能动的清单（除上述列出之外，其他全锁）

- 前端所有组件、CSS、打包产物。
- `main.py` 路由签名与 `/api/analyze_source`。
- 其他不变量（20px 网格对齐、自动保存等）。

---

## 4. 验收标准

- [x] 代码变动仅限于 `ai/wireframe_generator.py`。
- [x] 后端 `main.py` 能够正常运行无报错。
- [x] 导入任何项目（本地或 GitHub），能输出包含“核心项目结构树”与“数据流管道卡片（带箭头连线）”的双栏架构图。
- [x] 数据格式完美契合前端 React，不出现崩溃，且能在追加到画板后让元素完美关联。
