# BRIEF: <一句话任务描述>

> **使用方法**：复制本模板，重命名为 `BRIEF_<task>.md`（如 `BRIEF_add_flow_card.md`），D 填完后直接执行（或贴给 Gemini）。任务完成后删除该文件。
>
> **执行前必须先读 `ARCHITECTURE_BRIEF.MD` 第 1 / 5 / 9 / 11 节**——核心哲学、不变量、锁列表、红线。

---

## 1. 本次目标

<用一句话说清楚要实现什么效果。例：「需求画板加一个『流程图』卡片类型，可以画简单连线。」>

**回答这个：这是在帮用户更好地"想清楚"吗？还是在自动化掉用户的思考？**

<一句话理由>

## 2. 改动范围（精确到文件 + 函数）

**唯一允许修改的文件**（默认 `ui/index.html` + `ui/app.js`，后端改动需在这里写明）：
- `<file>`

**允许改的函数**（其他函数一律不动）：
- `<函数名>` — <要改成什么>
- `<函数名>` — <要改成什么>

**允许新增的函数**（如有）：
- `<函数名>` — <做什么>

**允许新增的 API**（如有，要更新 SOT 第 6 节）：
- `<METHOD> <path>` — <做什么>

**允许新增 / 修改的 saves JSON 字段**（如有）：
- `<key>` — <用途>

## 3. 不能动的清单（除上述列出之外，其他全锁）

- 所有未在第 2 节列出的函数
- 所有不变量（见 `ARCHITECTURE_BRIEF.MD` 第 5 节）
- 锁列表中的所有文件（见 `ARCHITECTURE_BRIEF.MD` 第 9 节）
- 现有 API 签名一律不许改（见 `ARCHITECTURE_BRIEF.MD` 第 6 节），可以加新的
- 不许 `git` 任何操作
- 不许新增 `fix_*.py` / `apply_*.py` / `analyze_*.py` 这类补丁脚本
- 不许重写整个文件

## 4. 验收标准

D 验收会检查：
- [ ] 改动只在第 2 节列出的范围内
- [ ] `python3 main.py` 能跑起来，浏览器能打开页面，没报错
- [ ] 功能行为：<列出 1-3 条具体可测的行为>
- [ ] 回归：<列出本次最容易碰坏的现有功能，确保仍然好用，例如「新建/切换/删除项目」「auto-save」「Generate Spec」「线框图拖拽」>
- [ ] 数据兼容：旧的 `saves/*.json` 仍能正常加载（不破坏现有用户数据）

## 5. 提示 / 上下文（可选）

<相关函数的位置（行号）、需要参考的现有模式、已知坑点。>

---

## Handoff（执行完成后填这里）

- 改了哪些函数：
- 新增了什么：
- 有没有碰到红线（如果有，说明原因）：
- 自测结果：
